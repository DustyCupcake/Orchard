-- Indicators are no longer permission-tiered.
--
-- An indicator's whole purpose is to tell the community something about
-- itself, so gating the breakdown behind a coordination grant was the
-- wrong shape twice over: most members still saw the coverage line --
-- which is itself a set of facts about a small, nameable group -- and saw
-- none of the content. What protects this surface is consent
-- (allow_prefer_not_to_say is required on every published question, a
-- member can opt out of all indicators at once, and a pre-publication
-- answer is asked about before it is counted) plus the community's own
-- choice of what to publish, not a holder role.
--
-- Dropped rather than left unused, so the column can't be read as a
-- still-supported option. Coordination-gated visibility does have a real
-- use in this app; it just isn't here -- that belongs to the visibility
-- *category* a question sits in, not to a mode an aggregate can be in.
--
-- Generated by drizzle-kit (the column/type are dropped in that order,
-- which is the only order that works: the type still has a dependent
-- column until the first statement runs).
ALTER TABLE "profile_question" DROP COLUMN "indicator_visibility";--> statement-breakpoint
DROP TYPE "public"."indicator_visibility";